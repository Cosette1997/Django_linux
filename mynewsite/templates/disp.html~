 <!DOCTYPE html>
  <html>
   <head>
    <meta charset='utf-8'>
    <title>{{ product.name }}</title>
   </head>
   <body>
    <h2>{{ product.name }}</h2>
    <hr>
    <table width=400 border=1 bgcolor='#ccffcc'>
     <tr><td>Product ID</td><td>{{ product.sku }}</td></tr>
     <tr><td>Name</td><td>{{ product.name }}</td></tr>
     <tr><td>Price</td><td>{{ product.price }}</td></tr>
     <tr><td>Amount</td><td>{{ product.qty }}</td></tr>
    </table>
    <a href='/list'>Return</a>
   </body>
  </html>
